<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Color de fondo interno (opcional) -->
    <stroke
        android:width="2dp"
        android:color="@color/amarillo"/>
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp"/> <!-- Espacio entre el borde y el contenido -->
    <corners
        android:radius="8dp"/> <!-- Esquinas redondeadas (opcional) -->
</shape>